# Server
PORT=3000

# PostgreSQL Database
DATABASE_URL=postgresql://user:password@localhost:5432/muaplanner

# SMTP Email (Brevo, SendGrid, Gmail, etc.)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@yourdomain.com
